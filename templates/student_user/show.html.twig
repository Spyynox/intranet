{% extends 'base.html.twig' %}

{% block title %}StudentUser{% endblock %}

{% block body %}
    <h1>StudentUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_user.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('student_user_index') }}">back to list</a>

    <a href="{{ path('student_user_edit', {'id': student_user.id}) }}">edit</a>

    {{ include('student_user/_delete_form.html.twig') }}
{% endblock %}
