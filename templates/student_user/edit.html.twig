{% extends 'base.html.twig' %}

{% block title %}Edit StudentUser{% endblock %}

{% block body %}
    <h1>Edit StudentUser</h1>

    {{ include('student_user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('student_user_index') }}">back to list</a>

    {{ include('student_user/_delete_form.html.twig') }}
{% endblock %}
