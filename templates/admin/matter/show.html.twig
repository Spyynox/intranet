{% extends 'base.html.twig' %}

{% block title %}Matter{% endblock %}

{% block body %}
    <h1>Matter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ matter.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_index') }}">back to list</a>

    <a href="{{ path('admin_edit', {'id': matter.id}) }}">edit</a>

    {{ include('admin/matter/_delete_form.html.twig') }}
{% endblock %}
